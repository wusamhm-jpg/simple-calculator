# Simple Calculator

A simple Python calculator project to learn GitHub and GitHub Actions.
#Configrations
 git config --global user.name "wusamhm-jpg"
 git config --global user.email "wusa.mhm@gmail.com"
 git config --list

git init
git add .
git commit -m ""
git remote add origin (link)
git remote -v
git branch
git branch -M main
git branch
git push -u origin main


#ssh configration
ls ~/.ssh
ssh-keygen -t ed25519 -C "your_email@example.com"
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
cat ~/.ssh/id_ed25519.pub

git push -u origin main
git push

## Protected branch test
## CI/CD Training

This project uses GitHub Actions for automated testing.
